我将修复电源计划切换问题并添加用户反馈入口。

### 修复电源计划切换问题
1.  **改进错误处理**：目前的 `set_active_plan` 仅检查退出状态码，且在 UI 层如果失败没有任何提示。我将修改 `src/core/power.rs`，使其在失败时捕获并返回 `powercfg` 的错误输出（stderr），以便定位问题。
2.  **增强 UI 反馈**：修改 `src/ui/app.rs`，当切换失败时，将具体的错误信息显示在状态栏中，而不是静默失败。

### 添加反馈与加群功能
1.  **引入依赖**：添加 `webbrowser` 库，用于在点击按钮时打开浏览器。
2.  **更新 UI**：在 `src/ui/app.rs` 底部添加：
    *   文本提示：“反馈和更新获取请加群：629474892”
    *   按钮：“加入群聊【TN测试群】”，点击后跳转到 `https://qm.qq.com/q/oIKs1SQpMs`。

我将立即执行这些修改。