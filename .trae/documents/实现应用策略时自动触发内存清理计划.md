我将修改 `src/ui/app.rs`，在点击“应用”按钮的事件处理逻辑中，增加对 `governor::trim_memory(proc_pid)` 的调用。

### 计划详情
1.  **定位代码**：找到 `src/ui/app.rs` 中处理“应用”按钮点击的闭包（`if ui.button("应用").clicked()`）。
2.  **调用清理**：在 `tokio::spawn` 的异步任务块末尾，添加 `governor::trim_memory(proc_pid).await` 调用。
    *   这将复用 `src/core/governor.rs` 中已实现的 `trim_memory` 函数（即底层调用 `EmptyWorkingSet`）。
3.  **更新反馈**：修改 `self.status_msg`，提示用户策略已应用且**内存已清理**。

这样既满足了“自动触发”的需求，又严格遵守了“使用已写好代码”的原则。我将立即执行修改。