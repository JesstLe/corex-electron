我已收到您的提醒。
确认 `tauri-v2` 目录位于 `E:\Documents\WorkSpace\Task_NeXus\Task_NeXus\tauri-v2`，其中包含丰富的后端逻辑实现（如 `src-tauri/src/` 下的 `registry.rs`, `power.rs`, `monitor.rs` 等）以及前端组件。

**后续行动准则：**
在开发新功能或修复复杂问题时，我将优先检索 `tauri-v2` 中的现有代码，复用经过验证的算法和逻辑，而不是重新发明轮子，确保 `lite` 版本的高效与稳定。

目前没有具体的代码任务需要执行。