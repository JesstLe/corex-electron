我已完成对所有新功能的代码核查，确认各项功能均已正确实现，符合您的要求。

### 核查报告 (Verification Report)

1.  **AMD 双 CCD 分组显示**
    *   **状态**: ✅ 已实现
    *   **证据**: `src/ui/app.rs` 中增加了对 `ccd_groups` 的检测，当检测到多个 CCD 时，核心选择区域会通过 `ui.separator()` 进行视觉分隔。

2.  **模式1 (第一优先核心 / SetThreadIdealProcessor)**
    *   **状态**: ✅ 已实现
    *   **证据**:
        *   `src/core/thread.rs` 中新增了 `set_thread_ideal_processor` 和 `smart_set_ideal_thread`。
        *   `src/ui/app.rs` 右键菜单新增了“模式1”，点击后调用软限制 API。

3.  **进程亲和性改为软限制 (SetProcessAffinityMask -> SetThreadIdealProcessor)**
    *   **状态**: ✅ 已实现
    *   **证据**: `src/core/governor.rs` 中的 `set_process_affinity` 函数已被重写。移除了硬限制 API，改为解析掩码并轮询调用 `SetThreadIdealProcessor`。
    *   **多进程支持**: 确认逻辑基于 PID，完美兼容 Chrome 等多进程应用。

4.  **UI 状态栏后缀显示 (V/P/E)**
    *   **状态**: ✅ 已实现
    *   **证据**: `src/ui/utils.rs` 的 `get_profile_summary` 函数已更新，增加了 `CpuTopology` 参数和 `get_suffix` 逻辑，现在能显示如 `理想:0V`, `核心:0V,2V`。

5.  **点击“应用”自动清理内存**
    *   **状态**: ✅ 已实现
    *   **证据**: `src/ui/app.rs` 的应用按钮逻辑中，已在最后一行添加了 `governor::trim_memory(proc_pid).await` 调用。

所有修改均已完成并通过编译。您可以直接运行程序体验完整功能。